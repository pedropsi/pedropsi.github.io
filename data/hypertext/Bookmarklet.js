HyperText("Bookmarklet",function(){
	return `
		<h3>In Chrome</h3>
		<p>${KB("Drag")} the above bookmarklet link to your bookmarks toolbar.</p>
		
		<h3>In Firefox</h3>
		<p>${KB("rightclick")} on the above bookmarklet link, then select "bookmark this link".</p>

		<h3>If nothing else works</h3>
		<p>Replace the content of an old bookmark with the above link content. Or you could run the bookmarklet js code directly in a game page (it'd still be quicker than manual alternatives).</p>
	`
})
