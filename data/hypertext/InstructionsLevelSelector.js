HyperText("InstructionsLevelSelector",function(){
	return `
	<h3>Level selector</h3>
	<h4>Opening the selector menu</h4>
	<p>Press ${v.LEVEL_KEY()} or the ${KB("Level selector")}.</p>
	${HyperText("InstructionsLevelSelectorPick")}
	`;
})
